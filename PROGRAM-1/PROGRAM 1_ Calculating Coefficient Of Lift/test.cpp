#include <iostream>
#include <fstream>
#include <sstream>
using namespace std;

int main (){
    int i;
    ifstream fin("text1.txt");
    ifstream inFS ("text.txt");

    sin.str()

    if (!fin.is_open()) {
        return -1;
    }

    fin >> i;

    //fin _ \n delimiters
    //getline(fin, i, ',') \n
    //fin.get() no delimiter

    //fout << i
    //endl
}